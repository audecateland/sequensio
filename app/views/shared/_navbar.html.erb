<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <div class="container-fluid">
    <%= link_to image_tag("logo-sequensio.png", border: 0), root_path, id: :logo, class: "navbar-brand", style: "margin:0 auto;" %>
  </div>
  <div class="container-fluid p-0">
    <div class="user">
      <% if user_signed_in? %>
        <p><%= current_user.email %></p>
      <% end %>
    </div>
    <button class="navbar-toggler menu-burger" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><%= link_to 'Accueil Sequensio', music_sessions_path(@music_session) %></li>
        <li class="nav-item"><a class="nav-link">Mes sessions préférées</a></li>
        <li class="nav-item"><%= button_to "Déconnexion", destroy_user_session_path(), method: :delete %></li>
      </ul>
    </div>
  </div>
</nav>
