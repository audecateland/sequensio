<div class="container">
  <div class="row">
    <div class="col-8">
      <% @tracks.each do |track| %>
        <p><%= track.title %></p>
        <p><%= track.artist %></p>
        <p><%= track.duration_track %></p>
      <% end %>
    </div>
  </div>
</div>
